[project]
name = "curtailment-intelligence"
version = "0.9.0"
description = "Curtailment risk, explainability and mitigation decision-support platform."
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
addopts = "-q"

[tool.ruff]
line-length = 100
