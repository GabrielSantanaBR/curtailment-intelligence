# Optional React frontend

The production-style demo UI lives in `/web` and requires no npm build. This folder is the starting point for the frontend developer if the team wants a React/Vite application during the competition.

Suggested contract: keep all data access behind `/api/v1/*`; do not import ML logic into the browser.
