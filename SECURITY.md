# Security notes

This is a hackathon prototype, not a production SCADA/EMS component. It must not be connected directly to operational control systems. Treat uploaded datasets as untrusted input, keep secrets out of the repository, and deploy the demo behind normal platform access controls if internet-exposed.
